const storeClient=window.MIAuth?.client;const productGrid=document.getElementById("productGrid"),cartDrawer=document.getElementById("cartDrawer"),cartOverlay=document.getElementById("cartOverlay"),cartItems=document.getElementById("cartItems"),cartCount=document.getElementById("cartCount"),cartTotal=document.getElementById("cartTotal"),checkoutBtn=document.getElementById("checkoutBtn");let products=[],cart=[];
const STORE_IMAGES=["https://www.mumbaiindians.com/static-assets/waf-images/c7/6e/c1/16-9/592-444/DAtzKSYFre.jpg","https://www.mumbaiindians.com/static-assets/waf-images/29/3f/27/16-9/592-444/6fIwhgYlS6.png","https://www.mumbaiindians.com/static-assets/waf-images/a0/1e/de/16-9/592-444/0eoBNvu0zU.jfif","https://www.mumbaiindians.com/static-assets/waf-images/99/b2/81/16-9/1035-512/p87Sq8tw21.jpg"];
const FALLBACK_PRODUCTS=[{id:"fan-jersey",name:"Mumbai Indians Fan Jersey",category:"JERSEY",price:1499,description:"Blue match-day inspired fan jersey for the One Family.",image:STORE_IMAGES[0],stock:20},{id:"mi-cap",name:"Mumbai Indians Cap",category:"CAP",price:699,description:"A classic MI cap for everyday fan style.",image:STORE_IMAGES[1],stock:20},{id:"mi-mug",name:"Mumbai Indians Mug",category:"LIFESTYLE",price:399,description:"Start your day with Mumbai Indians pride.",image:STORE_IMAGES[2],stock:20},{id:"mi-flag",name:"Mumbai Indians Flag",category:"FAN GEAR",price:499,description:"Bring the Blue Army atmosphere home.",image:STORE_IMAGES[3],stock:20}];
function escapeHTML(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}function formatPrice(v){return new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(Number(v)||0)}function getProductImage(p){return p.image_url||p.image||""}function loadCart(){try{const s=JSON.parse(localStorage.getItem("mi_cart")||"[]");cart=Array.isArray(s)?s.filter(i=>i&&i.id!=null&&Number(i.quantity)>0):[]}catch{cart=[]}renderCart()}function saveCart(){localStorage.setItem("mi_cart",JSON.stringify(cart))}
async function loadProducts(){if(!productGrid)return;productGrid.innerHTML='<div class="loading-products"><i class="fa-solid fa-spinner fa-spin"></i><p>Loading MI Collection...</p></div>';try{if(storeClient){const{data,error}=await storeClient.from("products").select("*").eq("is_active",true).order("created_at",{ascending:false});if(!error&&Array.isArray(data)&&data.length)products=data}}catch(e){console.error("Product Load Error:",e)}if(!products.length)products=FALLBACK_PRODUCTS;renderProducts();reconcileCartStock()}
function renderProducts(){productGrid.innerHTML="";products.forEach(product=>{const stock=Math.max(0,Number(product.stock)||0),image=getProductImage(product),card=document.createElement("article");card.className="product-card";card.innerHTML=`<div class="product-image"><span class="product-badge">MI COLLECTION</span>${image?`<img src="${escapeHTML(image)}" alt="${escapeHTML(product.name)}" class="product-real-image">`:`<div class="product-placeholder"><i class="fa-solid fa-shirt"></i></div>`}</div><div class="product-content"><p class="product-category">${escapeHTML(product.category||"MI MERCHANDISE")}</p><h3>${escapeHTML(product.name||"MI Product")}</h3><p class="product-description">${escapeHTML(product.description||"Mumbai Indians fan merchandise.")}</p><div class="product-bottom"><strong>${formatPrice(product.price)}</strong><button type="button" class="buy-btn" ${stock<=0?"disabled":""}>${stock>0?"Add to Cart":"Out of Stock"} <i class="fa-solid fa-cart-plus"></i></button></div></div>`;const imageEl=card.querySelector("img");imageEl?.addEventListener("error",()=>imageEl.replaceWith(Object.assign(document.createElement("div"),{className:"product-placeholder",innerHTML:'<i class="fa-solid fa-shirt"></i>'})));card.querySelector(".buy-btn")?.addEventListener("click",()=>addToCart(product.id));productGrid.appendChild(card)})}
function reconcileCartStock(){cart=cart.map(item=>{const p=products.find(x=>String(x.id)===String(item.id));if(!p)return null;const stock=Math.max(0,Number(p.stock)||0);return{...item,name:p.name,category:p.category,price:Number(p.price)||0,image:getProductImage(p),quantity:Math.min(Number(item.quantity)||1,stock)}}).filter(Boolean).filter(i=>i.quantity>0);saveCart();renderCart()}function addToCart(id){const p=products.find(x=>String(x.id)===String(id));if(!p)return;const stock=Math.max(0,Number(p.stock)||0),existing=cart.find(x=>String(x.id)===String(id));if(existing){if(existing.quantity>=stock)return;existing.quantity++}else cart.push({id:p.id,name:p.name,category:p.category||"MI MERCHANDISE",price:Number(p.price)||0,image:getProductImage(p),quantity:1});saveCart();renderCart();openCart()}function changeQuantity(id,change){const item=cart.find(x=>String(x.id)===String(id));if(!item)return;const p=products.find(x=>String(x.id)===String(id)),stock=p?Math.max(0,Number(p.stock)||0):Number.MAX_SAFE_INTEGER;item.quantity+=change;if(item.quantity>stock)item.quantity=stock;if(item.quantity<=0)cart=cart.filter(x=>String(x.id)!==String(id));saveCart();renderCart()}function removeFromCart(id){cart=cart.filter(x=>String(x.id)!==String(id));saveCart();renderCart()}
function renderCart(){if(!cartItems)return;const total=cart.reduce((s,i)=>s+Number(i.quantity||0),0),amount=cart.reduce((s,i)=>s+Number(i.price||0)*Number(i.quantity||0),0);cartCount.textContent=total;cartTotal.textContent=formatPrice(amount);if(!cart.length){cartItems.innerHTML='<div class="empty-cart"><i class="fa-solid fa-cart-shopping"></i><h3>Your cart is empty</h3><p>Add some MI gear to continue.</p></div>';return}cartItems.innerHTML="";cart.forEach(item=>{const row=document.createElement("div");row.className="cart-item";row.innerHTML=`<div class="cart-item-icon">${item.image?`<img src="${escapeHTML(item.image)}" alt="">`:`<i class="fa-solid fa-shirt"></i>`}</div><div class="cart-item-info"><h4>${escapeHTML(item.name)}</h4><p>${formatPrice(item.price)}</p><div class="quantity-control"><button type="button" data-action="decrease">−</button><span>${item.quantity}</span><button type="button" data-action="increase">+</button></div></div><div class="cart-item-right"><strong>${formatPrice(Number(item.price)*Number(item.quantity))}</strong><button type="button" data-action="remove" aria-label="Remove item"><i class="fa-solid fa-trash"></i></button></div>`;row.querySelector('[data-action="decrease"]').onclick=()=>changeQuantity(item.id,-1);row.querySelector('[data-action="increase"]').onclick=()=>changeQuantity(item.id,1);row.querySelector('[data-action="remove"]').onclick=()=>removeFromCart(item.id);cartItems.appendChild(row)})}function openCart(){cartDrawer?.classList.add("open");cartOverlay?.classList.add("show");document.body.classList.add("cart-open")}function closeCart(){cartDrawer?.classList.remove("open");cartOverlay?.classList.remove("show");document.body.classList.remove("cart-open")}async function checkout(){if(!cart.length)return;const user=await window.MIAuth?.getCurrentUser();if(!user){window.location.href="login.html?redirect=store.html";return}window.location.href="checkout.html"}document.getElementById("cartNavBtn")?.addEventListener("click",openCart);document.getElementById("closeCartBtn")?.addEventListener("click",closeCart);cartOverlay?.addEventListener("click",closeCart);checkoutBtn?.addEventListener("click",checkout);document.addEventListener("keydown",e=>{if(e.key==="Escape")closeCart()});loadCart();loadProducts();
